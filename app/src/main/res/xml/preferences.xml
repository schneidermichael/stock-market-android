<?xml version="1.0" encoding="utf-8"?>
<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <Preference
        app:key="deleteAllStocks"
        app:title="@string/preference_delete_all" />

    <ListPreference
        android:defaultValue="asc"
        android:entries="@array/pref_sort_titles"
        android:entryValues="@array/pref_sort_values"
        android:key="pref_sort"
        android:negativeButtonText="@null"
        android:positiveButtonText="@null"
        android:title="@string/preference_sort"
        android:summary="%s"/>

</PreferenceScreen>